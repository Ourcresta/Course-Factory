# ============================================
# OUSHIKSHA GURU - PRODUCTION ENVIRONMENT
# Version 1.0 - MilesWeb Deployment
# ============================================

# ==================== DATABASE ====================
# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Individual PostgreSQL credentials (optional - for debugging)
PGHOST=hostname
PGPORT=5432
PGUSER=username
PGPASSWORD=your_secure_password
PGDATABASE=database_name

# ==================== APPLICATION ====================
# Environment mode
NODE_ENV=production

# Server port (default: 5000)
PORT=5000

# Application URL (your production domain)
APP_URL=https://yourdomain.com

# ==================== SECURITY ====================
# Session secret - Generate with: openssl rand -hex 64
SESSION_SECRET=your_64_character_random_hex_string_here

# JWT secret for token signing - Generate with: openssl rand -hex 32
JWT_SECRET=your_32_character_random_hex_string_here

# JWT expiration time
JWT_EXPIRES_IN=12h

# CORS allowed origins (comma-separated)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ==================== AI SERVICES ====================
# OpenAI API key for AI course generation
OPENAI_API_KEY=sk-your_openai_api_key_here

# OpenAI model (default: gpt-4o)
OPENAI_MODEL=gpt-4o

# ==================== EMAIL SERVICES ====================
# Resend API for email delivery (OTP, notifications)
RESEND_API_KEY=re_your_resend_api_key_here

# From email address (must be verified in Resend)
RESEND_FROM_EMAIL=noreply@yourdomain.com

# ==================== FILE STORAGE ====================
# Max upload size in MB
MAX_UPLOAD_SIZE_MB=10

# Upload directory path
UPLOAD_DIR=./uploads

# ==================== RATE LIMITING ====================
# API rate limit (requests per window)
RATE_LIMIT_MAX=100

# Rate limit window in minutes
RATE_LIMIT_WINDOW_MINUTES=15

# ==================== LOGGING ====================
# Log level: error, warn, info, debug
LOG_LEVEL=info

# Log file path
LOG_FILE=./logs/app.log

# ==================== PUBLIC API (Shishya Portal) ====================
# Enable public API endpoints
ENABLE_PUBLIC_API=true

# API rate limit for public endpoints
PUBLIC_API_RATE_LIMIT=50

# ============================================
# IMPORTANT SECURITY NOTES:
# 1. Never commit this file with real values
# 2. Use strong, unique passwords
# 3. Rotate secrets regularly
# 4. Enable HTTPS in production
# 5. Backup database regularly
# ============================================
